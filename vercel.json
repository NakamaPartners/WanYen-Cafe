{
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/wanyen run build",
  "outputDirectory": "artifacts/wanyen/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
